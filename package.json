{
  "name": "client-a-w-fencing-quiz",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build-pages.mjs",
    "check": "node --check public/config.js && node --check functions/api/capi.js && npm run build && node scripts/check-site.mjs",
    "dev": "npm run build && wrangler pages dev dist",
    "deploy": "npm run build && wrangler pages deploy dist --project-name client-a-w-fencing-quiz --branch main"
  },
  "devDependencies": {
    "wrangler": "^4.35.0"
  }
}
